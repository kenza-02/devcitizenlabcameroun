---
import { FaHandshake } from "react-icons/fa";
import { FaUsers } from "react-icons/fa6";
import { HiOutlineLightBulb } from "react-icons/hi";

const stats = [
  {
    id: 1,
    title: "Jeunes formés",
    amount: "150+",
    description:
      "aux outils numériques pour s'engager activement dans la transformation de leur communauté",
    icon: FaUsers,
    color: "green",
    gradient: "from-green-500 to-emerald-600",
  },
  {
    id: 2,
    title: "Projets réalisés",
    amount: "10",
    description:
      "des idées transformées en vraies solutions concrètes qui changent des vies",
    icon: HiOutlineLightBulb,
    color: "yellow",
    gradient: "from-yellow-500 to-amber-600",
  },
  {
    id: 3,
    title: "Partenariats",
    amount: "5",
    description:
      "avec des alliés engagés pour amplifier notre impact et créer le changement",
    icon: FaHandshake,
    color: "red",
    gradient: "from-red-500 to-rose-600",
  },
];
---

<section
  class="relative py-20 bg-gradient-to-br from-white via-gray-50 to-green-50/30 overflow-hidden"
>
  <!-- Formes décoratives animées -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div
      class="shape-blob absolute -top-40 -right-40 w-96 h-96 bg-gradient-to-br from-green-200/30 to-green-300/20 rounded-full blur-3xl"
    >
    </div>
    <div
      class="shape-blob absolute -bottom-40 -left-40 w-[500px] h-[500px] bg-gradient-to-br from-red-200/20 to-yellow-200/20 rounded-full blur-3xl"
    >
    </div>
    <div
      class="shape-blob absolute top-1/2 left-1/3 w-64 h-64 bg-gradient-to-br from-yellow-200/20 to-green-200/20 rounded-full blur-2xl"
    >
    </div>
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- En-tête avec animation -->
    <div class="text-center mb-20 space-y-6">
      <div class="inline-block animate-fade-in-down">
        <span
          class="px-5 py-2 border border-green-600 text-green-600 bg-white rounded-full text-xs font-semibold tracking-wide uppercase"
        >
          Notre Impact
        </span>
      </div>
      <h2 class="text-4xl md:text-5xl font-bold mb-6">
        Pourquoi <span class="text-green-600">CitizenLab Sénégal</span> ?
      </h2>
      <p
        class="text-lg md:text-xl text-center text-gray-700 max-w-3xl mx-auto leading-relaxed"
      >
        Chez CitizenLab Sénégal, on croit que chaque voix compte. Notre mission
        ? Offrir un espace collaboratif où citoyens, innovateurs et acteurs du
        changement imaginent ensemble des solutions numériques pour construire
        un Sénégal plus juste, transparent et inclusif. Parce que les grandes
        idées méritent d’être entendues — et concrétisées.
      </p>
    </div>

    <!-- Statistiques en layout moderne non-card -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
      {
        stats.map((stat) => (
          <div class="stat-item bg-white/80 backdrop-blur-md p-8  text-start">
            <h3 class="text-7xl text-green-700 font-extrabold  mb-2">
              {stat.amount}
            </h3>
            <h4 class="text-2xl text-green-800 font-semibold  mb-2">
              {stat.title}
            </h4>
            <p class="text-gray-600 leading-relaxed">{stat.description}</p>
          </div>
        ))
      }
    </div>

    <!-- CTA -->
    <div class="text-center mt-12">
      <a
        href="/a-propos"
        class="inline-flex items-center gap-2 px-8 py-3 text-lg font-semibold text-white bg-green-600 hover:bg-green-700 rounded-lg shadow-lg hover:shadow-xl transition-all"
      >
        En savoir plus
        <svg
          class="w-5 h-5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
        </svg>
      </a>
    </div>
  </div>
</section>
